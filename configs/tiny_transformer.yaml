# Dense baseline (FlashAttention if available). Used from Phase 4 onward.
arch: transformer
vocab_size: 99      # set by tokenizer (Phase 3); placeholder
d_model: 256
n_layers: 6
n_heads: 4
d_ff: 512
max_seq_len: 512    # train short; eval long (OOD)
dropout: 0.0
