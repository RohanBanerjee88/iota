"""Tiny deterministic tokenizer — STUB (Phase 3).

Not implemented yet. Per BUILD_PLAN.md the first session stops after the Phase 2
acceptance gate. The vocabulary building blocks already live in `dsl.py`
(KEYWORDS, OPERATORS, VAR_NAMES, NOISE_TOKENS, ACC) and numbers will be
digit-split, keeping the vocab well under ~100 tokens.
"""

raise NotImplementedError("tokenizer is Phase 3; not built in the first session")
