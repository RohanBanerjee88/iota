# Minimal deps. Iteration speed beats sophistication.
torch
numpy
pyyaml
matplotlib
tqdm
# flash-linear-attention is OPTIONAL and isolated behind try/except.
# Install separately if you want the GLA reference check; never a hard dep.
# flash-linear-attention
